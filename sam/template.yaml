AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31

Description: ComplAI Lambda – local SAM development template

Parameters:
  Environment:
    Type: String
    Default: development
    AllowedValues:
      - development
      - production
    Description: Deployment environment (development or production)
  LambdaRegion:
    Type: String
    Default: eu-west-1
    Description: AWS region for Lambda and S3
  OpenRouterApiKey:
    Type: String
    Description: OpenRouter API key (passed from CI or manually)
  JwtSecret:
    Type: String
    Description: Base64-encoded HS256 JWT secret (min 32 bytes). Generate with openssl rand -base64 32.

Conditions:
  IsProduction: !Equals [!Ref Environment, production]

Globals:
  Function:
    Timeout: 60
    MemorySize: 768
    Runtime: java21
    Environment:
      Variables:
        OPENROUTER_API_KEY: !Ref OpenRouterApiKey
        AWS_ENDPOINT_URL: "http://localhost:4566"

Resources:
  ComplAILambdaRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub ComplAILambdaRole-${Environment}
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      Description: !Sub IAM role for ComplAI Lambda (${Environment}) with least privilege
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole

  ComplAILogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      # Use !Sub /aws/lambda/${ComplAIFunction} to ensure logs go to the function's actual log group
      LogGroupName: !Sub /aws/lambda/${ComplAIFunction}
      RetentionInDays: !If [IsProduction, 90, 7]

  ComplAIErrorMetricFilter:
    Type: AWS::Logs::MetricFilter
    Properties:
      LogGroupName: !Ref ComplAILogGroup
      FilterPattern: "{ $.errorCode = * }"
      MetricTransformations:
        - MetricNamespace: !Sub ComplAI/${Environment}
          MetricName: AuditErrorCount
          MetricValue: 1
          DefaultValue: 0
          Dimensions:
            - Key: endpoint
              Value: $.endpoint
            - Key: errorCode
              Value: $.errorCode

  ComplAILatencyMetricFilter:
    Type: AWS::Logs::MetricFilter
    Properties:
      LogGroupName: !Ref ComplAILogGroup
      FilterPattern: "{ $.latencyMs = * }"
      MetricTransformations:
        - MetricNamespace: !Sub ComplAI/${Environment}
          MetricName: AuditLatencyMs
          MetricValue: $.latencyMs
          DefaultValue: 0
          Dimensions:
            - Key: endpoint
              Value: $.endpoint

  ProceduresBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub complai-procedures-${Environment}
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      LifecycleConfiguration:
        Rules:
          - Status: Enabled
            ExpirationInDays: 365
      DeletionPolicy: Retain

  ComplAIFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../build/libs/complai-all.jar
      Handler: io.micronaut.function.aws.proxy.payload2.APIGatewayV2HTTPEventFunction::handleRequest
      Runtime: java21
      MemorySize: 768
      Timeout: 60
      Role: !GetAtt ComplAILambdaRole.Arn
      Environment:
        Variables:
          OPENROUTER_API_KEY: !Ref OpenRouterApiKey
          PROCEDURES_BUCKET: !Ref ProceduresBucket
          PROCEDURES_KEY: procedures.json
          PROCEDURES_REGION: !Ref LambdaRegion
          AWS_ENDPOINT_URL: "http://host.docker.internal:4566"
          OPENROUTER_REQUEST_TIMEOUT_SECONDS: "60"
          OPENROUTER_OVERALL_TIMEOUT_SECONDS: "60"
          OPENROUTER_MODEL: google/gemma-3-27b-it:free
          JWT_SECRET: !Ref JwtSecret
      Events:
        ProxyRoot:
          Type: HttpApi
          Properties:
            Path: /
            Method: ANY
        ProxyGreedy:
          Type: HttpApi
          Properties:
            Path: /{proxy+}
            Method: ANY

Outputs:
  ComplAILambdaFunctionName:
    Description: Name of the deployed ComplAI Lambda function
    Value: !Ref ComplAIFunction
  ComplAILambdaArn:
    Description: ARN of the deployed ComplAI Lambda function
    Value: !GetAtt ComplAIFunction.Arn
