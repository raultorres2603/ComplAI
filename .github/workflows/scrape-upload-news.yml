name: Scrape and Upload News

# Triggered manually from the GitHub Actions UI.
# Crawls a city's news website using the corresponding mapping file
# (src/main/resources/scrapers/procedures-mapping-<city>.json news section),
# produces news-<city>.json, and uploads it to the news S3 bucket.
#
# AWS credentials are obtained via OIDC (github-cdk-deploy-role). No static key
# secrets are required. The target bucket is derived automatically from the chosen environment:
#   complai-news-development  /  complai-news-production

permissions:
  id-token: write
  contents: read

on:
  workflow_dispatch:
    inputs:
      city:
        description: >
          City identifier — must match a
          src/main/resources/scrapers/procedures-mapping-<city>.json file
          that contains a news section.
        required: true
        type: string
        default: elprat
      environment:
        description: 'Target deployment environment'
        required: true
        type: choice
        options:
          - development
          - production
        default: development

jobs:
  scrape-and-upload:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Java 25
        uses: actions/setup-java@v5
        with:
          java-version: '25'
          distribution: 'temurin'

      - name: Cache Gradle packages
        uses: actions/cache@v5
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle', 'gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Build fat JAR
        run: ./gradlew clean shadowJar

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: arn:aws:iam::134267836527:role/github-cdk-deploy-role
          aws-region: ${{ vars.NEWS_REGION }}
          audience: sts.amazonaws.com
          output-env-credentials: true

      - name: Scrape news and upload to S3
        env:
          NEWS_BUCKET: complai-news-${{ inputs.environment }}
          NEWS_REGION: ${{ vars.NEWS_REGION }}
        run: |
          java -cp build/libs/complai-all.jar \
            cat.complai.scrapper.NewsScraper "${{ inputs.city }}"
