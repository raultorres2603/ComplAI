# Allow the Micronaut blocking HTTP client to read binary (non-text) response bodies.
# Without this the client codec closes the connection on application/pdf responses.

# Enable local CORS filter for testing
complai.local-cors-enabled=true

# CORS allowed origin for testing
complai.cors.allowed-origin=http://localhost:5173

# API key authentication — normally enabled, but tests can override
# Set to false globally; individual tests that need it can override via @Environment
api.key.enabled=false

# Disable response caching in tests to prevent test pollution
# Different test scenarios with the same question category should not share cached responses
response.cache.enabled=false

# Keep integration tests from hitting per-user throttling while validating many scenarios.
complai.rate-limit.requests-per-minute=200

rag.ambiguity.score-ratio-threshold=0.85

# SES Configuration for Testing
# Use non-sensitive test email addresses. These are NOT used for actual SES calls in unit tests
# (SES client is typically mocked), but are required for integration tests or manual testing.
# Test values do NOT contact real AWS SES, no credentials involved.
aws.ses.from-email=test-sender@example.local
aws.ses.region=eu-west-1
aws.ses.recipient-email=test-recipient@example.local
