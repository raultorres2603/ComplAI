# OpenRouter integration test environment configuration
# Disable the production ApiKeyAuthFilter; the test inner class TestApiKeyFilterOpenRouter
# provides its own @ServerFilter with hard-coded test keys.
api.key.enabled=false
