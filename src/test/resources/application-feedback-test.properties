# Feedback test environment configuration
# Disable the production ApiKeyAuthFilter; the test inner class TestApiKeyFilterFeedback
# provides its own @ServerFilter with hard-coded test keys.
api.key.enabled=false
