{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Local SAM",
            "type": "shell",
            "command": "./start-local.sh",
            "options": {
                "cwd": "${workspaceFolder}/sam"
            },
            "detail": "Starts LocalStack, builds the shadow JAR, and runs the SAM local API + SQS worker poller + SES poller for local development.",
            "presentation": {
                "echo": true,
                "reveal": "always",
                "focus": true,
                "panel": "new"
            },
            "problemMatcher": []
        }
    ]
}